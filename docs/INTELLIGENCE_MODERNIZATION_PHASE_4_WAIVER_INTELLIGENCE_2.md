# Intelligence Modernization — Phase 4
## Waiver Intelligence 2.0

Branch `intelligence-modernization-phase4-waiver-2`. **Not merged, not deployed.** Waiver 2.0 is a **SHADOW_ONLY** candidate engine; production waiver output (`waiver-engine-2026.1`) is preserved byte-for-byte. Read-only: it never submits, schedules or implies a claim.


## 1. Starting state
- `main` = `origin/main` = `11bc0a6` (Phase 3.5D record); branch `intelligence-modernization-phase4-waiver-2`. Drift checked after every checkpoint commit: 0 commits on `origin/main` not on the branch each time. Production health 200.
- Served versions: FI `fi:2026:w02:bfd77c9959a6` (week 2 PARTIAL), Role `roi:2026:w01:819dc3166607`, OPP `opi:2026:w01:bef17990fe91`, Player-Scheme `psi:2025:w18:08123edd58c9`, Start/Sit `ri-startsit-2026.1` (SHADOW_ONLY, frozen, sha `85d2ddd5…`).
- Waiver engine `waiver-engine-2026.1`. **Production waiver readiness: `UNAVAILABLE` (`FREE_AGENT_POOL_UNAVAILABLE`)** — the canonical `free_agent_pool` capability is not materialized (`snap.waiver_state == null`); the live `/api/intelligence/.../week/2` returns zero waiver recommendations.
- Book-Ready `book-ready-evidence-2026.1`; registry `intelligence-surface-registry-2026.2` (→ `2026.3` here); Analysis Book `analysis-book-2026.1` / `analysis-taxonomy-2026.1` (→ `2026.2`) / `analysis-planner-rules-2026.1`.
- Scoring identity: `scoring_fingerprint` (canonical snapshot lineage, `lib/canonical/scoring-fingerprint.ts`).

## 2. Existing waiver audit (`lib/weekly/waivers.ts`, 578 lines, `waiver-engine-2026.1`)
**What v1 optimizes.** For every prefiltered free agent it evaluates the FULL (add, drop) pair on a *counterfactual optimal legal lineup* (the shared Hungarian optimizer), picks the best legal drop by `(lineup gain − keep value)`, and ranks by `net_roster_gain = Σ weight·component` (`decision-score.ts`): `starter_upgrade·1.0 + weekly_vor·0.6 + bench_utility·0.25 + positional_scarcity·0.35 + bye_coverage·0.8 + injury_hedge·0.7 + rest_of_season_value·0.45 − drop_cost·1.0 − uncertainty_penalty·1.0`. Priority: `net < 0.75` → DO_NOT_ADD; `net ≥ 4` with a starter/bye/hedge reason → HIGH; `net ≥ 2` → MEDIUM; else LOW. It concludes DO_NOT_ADD, refuses a drop that leaves a slot unfillable, reports `starter_impact_status UNRESOLVED` (never a fake 0) on a projection gap, and is gated on `free_agent_pool_readiness.actionable`.
**Useful certified behavior preserved/consumed (not rebuilt):** the (add, drop) framing; optimal-lineup counterfactuals and their legality rule; UNRESOLVED-not-zero; the readiness gate; `WeeklyTeamContext`; the `weeklyVOR` replacement frontier; deterministic `ctx.generated_at`; canonical scoring via `scoreWeeklyLine`.
**Weaknesses (documented, v1 left untouched):** (1) generic player value — Role, OPP and FI are **not consumed** (`football_intelligence_used_for_numeric_ranking` is a literal `false`); (2) hard-coded unvalidated weights (table above, `clamp(-4,4)`, `keep = vor + 1.2·rosEdge + …`, `MIN_NET 0.75`); (3) drop cost is one scalar — no option value, handcuff/contingency, bye cover, positional depth or re-add probability; (4) no horizons beyond "this week" and one prorated ROS number; (5) no economics — *"FAAB bid sizing is a later phase"*: no budgets, competition, priority cost or visible winning bids; (6) a flat ranked top-8, no alternatives/tiers; (7) one league-pool replacement level; (8) uncertainty is a single `−1.5` flag; (9) the counterfactual lineup helper is a closure inside v1 (Waiver 2.0 calls the shared optimizer instead of copying v1's rules); (10) no historical validation or prospective capture.
**Surfaces:** `GET /api/waivers/{league}/{manager}/week/{week}`, `/api/intelligence`, the orchestrator (`candidates.ts`, `gates.ts`), Book-Ready `waiver.status` (thin, UNAVAILABLE per readiness), Analysis Book waiver chapters. The trade engine is separate and untouched.
**Answer:** v1 optimizes *the net immediate-to-near-term lineup and depth value of an add/drop pair on this roster*, from external weekly/ROS projections and hand-set weights. It is roster-aware but role-blind, market-blind, horizon-blind and price-blind.

## 3. The decision definition
The unit is the **transaction**: `WaiverAction = { manager, candidate, drop (or open spot), acquisition cost {FAAB range | priority}, gross_add_value, drop_cost, acquisition_cost, risk_penalty, net_action_value (+ interval), horizons {next_week, next_3, ros, playoffs, stash}, value_by_kind {starter, bench_option, contingency_payoff, contingency_counted}, exact next-week lineup delta, candidate & drop assets (role, conditional OPP, schedule, uncertainty, archetype), market {scarcity, competitors, faab, priority}, alternatives, tier A/B/C/PASS, confidence, components, content_id }`. **PASS ("make no claim") is a first-class action.** Nothing is submitted.

## 4. Architecture (`lib/waiver2/`, SHADOW_ONLY)
`config.ts` (every tunable, with status) → `types.ts` → `context.ts` (evaluation context) → `value.ts` (asset value) → `roster.ts` (lineup counterfactual, drop cost) → `market.ts` (competition, FAAB, priority) → `actions.ts` (`evaluateWaiver2`) → `adapter.ts` (real canonical state → input) · `capture.ts`, `evaluation.ts` (Checkpoint G). Book-Ready: `lib/book-ready/families/waiver2.ts` + three topics. No production module imports it.

## 5. Evaluation context (Checkpoint B)
`buildWaiverContext` runs ONCE per request from already-retrieved inputs. It owns the **Role cache** (per player), the **OPP cache** (per team + absence set), the canonical-scoring opportunity→points conversion, the baseline optimal lineup (built once), replacement pools, marginal starters and league-wide teams/budgets. `RetrievalCounters` prove reuse. The only clock is `weekly.generated_at` (a test scans the engine for wall-clock reads). Real Bloodline Bowl run: 105 assets valued, 770 Role lookups **vs 957 cache hits**, OPP cached, 451 lineup builds (bounded by the top-5 drop shortlist per candidate, not N×M).

## 6. Feature families
F1 production projection level (consumed); F2 Role level & trajectory (consumed); F3 conditional OPP; F4 schedule (bye, games remaining, playoff window, FI opponent readings); F5 roster context (exact lineup counterfactual, marginal starter, depth); F6 replacement pools; F7 market; F8 uncertainty. Every tunable is in `config.ts` with `status ∈ {PRIOR_UNVALIDATED, POLICY, DERIVED_FROM_LEAGUE}` and a `basis`; `PARAM_SNAPSHOT` hashes into lineage (`params_hash`).

## 7. Roster-value model
One function values ANY player against MY roster in league-scoring points per week, summed over horizons with a per-week discount. **Level** = current projection blended toward ROS/week over 4 weeks; + persisted Role change; + established OPP (next week only); × availability; bye = 0. **Margin** = what he must beat: for a candidate or my bench player, the marginal starter he could displace (from the optimal lineup: slot-eligibility aware, FLEX/SUPERFLEX correct); for my starter, his best bench cover. `starter_value = Σ max(0, level_w − margin)·avail·discount` over the 6-week decision horizon. The exact **next-week optimal-lineup counterfactual** replaces the asset model's next-week estimate whenever it differs by >1 point (lineup stays the source of truth; UNRESOLVED on a projection gap, never 0).

## 8. Replacement value
Per position: obtainable free-agent replacement (consumed nth-best-available framework), my starter baseline, my rostered replacement, bench replacement, and **scarcity = 1/(1+free agents at or above my starter baseline)**. A candidate is valued above what this roster can actually obtain, not above zero. A replacement level derived from a *fallback* (no free agent exists at that position) is **not treated as obtainable** (`fa_basis`) — this fixed a real defect found during Checkpoint H (depth was being priced against an unobtainable level).

## 9. Drop cost
`drop_cost = starter value lost (vs best remaining cover) + bench option lost + established contingency lost + bye-cover lost − re-add credit`, every part exposed (`drop.*` components). Positional **depth** is priced as `weeks × P(needed | position) × (level − max(cover, obtainable replacement))` with position priors (QB .07 … RB .16). Example (test): with the natural drop `r4` (a spare RB), adding a free-agent RB nets **7.99**; make `r4` valuable and the engine drops `r3` instead and the net falls to **0.61** — the drop changes the action. A roster whose bench is all non-redundant, similarly valuable assets turns a positive-looking add negative (net < 1.5, no recommendation).

## 10. Optionality
Three separate kinds: **starter value**, **bench option** (role growth × weight, variance upside × P(start), depth-chart uncertainty, positional depth) and **contingency** (counted only when established, else reported as `contingency_payoff` with zero weight). Emerging or rookie players show bench-option value; they are not forced into a weekly-start frame.

## 11. Schedule / time horizon
Five horizons reported separately: `next_week`, `next_3`, `ros`, `playoffs`, `stash`. Byes come from the NFL schedule; games remaining and known playoff opponents are exposed. A veteran can win `next_week` while a rising player wins `playoffs` (tested); a two-week injury fill gains only its established next-week delta (`next_3` gains the same single week — the window is not extrapolated). Deep player-specific matchup value is **Phase 5**.

## 12. Opportunity Propagation integration (conditional)
OPP is consumed via `evaluateOpportunityPropagationScenario` and stays CONDITIONAL. It counts toward value **only when an official availability designation of a teammate establishes the condition**, weighted by the designation's chance of missing (OUT/IR 1.0, DOUBTFUL 0.8, QUESTIONABLE 0.25), for the next week only, and raises the `injury_contingency` uncertainty. Otherwise the payoff is reported as `opp.conditional_payoff` with **zero weight** (tested: gross value is identical with and without the unestablished scenario). OPP is never used to estimate whether anyone will be unavailable.

## 13. Football Intelligence integration
FI opponent readings (`def_pass/rush_epa_allowed`, percentile, predictive status) are **explanatory only**: `fi_numeric: false`, `fi_adjustment_points = 0` (a test pins this). Numeric use awaits validation (Phase 5 matchup work). FI's PARTIAL week and Role lag feed the `source_lag` uncertainty.

## 14. Scoring integration
Opportunity→points uses the **canonical scoring engine** (`scoreWeeklyLine` on a typical per-target and per-carry stat line with the league's `raw_scoring`): PPR vs standard changes a target's worth (tested: PPR target > standard target; carries unchanged; different `scoring_fingerprint`). Role growth is worth more in a PPR league. **Unsupported propagation (documented for Phase 6):** threshold/bonus categories that need a whole game line (e.g. 100-yard bonuses), position-specific scoring beyond rec/rush, and return-yard scoring for Role return shares are not converted by the per-opportunity path; weekly projections are already in league scoring.

## 15. Market competition
For each other team: marginal starter at the position (slot-aware), the candidate's gap over it, unfilled slots, starters with injury designations or a bye within 2 weeks, budget. Output `CompetitorView{need_strength, marginal_starter_gap, positional_weakness, budget, evidence}`. **Structural only: intended bids are never inferred**; competitors' unseen budgets are labelled, not guessed.

## 16. FAAB
A range — `min_useful / expected_competitive / aggressive / walk_away` (dollars) — not rank→percentage. Inputs: my surplus (net before acquisition cost), **my budget's shadow price** (points per dollar = my top-3 distinct alternatives' surplus ÷ remaining budget, so alternatives raise the price of any one claim), structural demand, candidate desirability (rank among my targets), and **visible winning bids** when present (`VISIBLE_WINNING_BIDS`) else an explicit `UNCALIBRATED_PRIOR`. Bounds: walk-away ≤ 0.9 × value bought and ≤ 60% of remaining; never above my budget. Example (test league): FA WR → `[$6, $14, $37, $60]`; add two comparable alternatives and the walk-away falls to **$36**; with visible winning bids the expected bid is far above the prior. The **acquisition cost in points** = expected competitive bid × shadow price. Rolling/reverse-standings leagues get `priority.advice ∈ {SPEND, HOLD}` and a priority cost instead.

## 17. Action score
`net_action_value = gross_add_value − drop_cost − acquisition_cost − risk_penalty (+ exact lineup adjustment)`; `gross_add_value = starter + bench_option + established contingency` over the 6-week decision horizon; `risk_penalty = 0.6 × uncertainty × gross`. Every term is a named component; tests assert the components sum to the net. Tiers: **A** net ≥ 3 and lower bound > 0; **B** ≥ 1.25; **C** ≥ 0.75 (speculative); **PASS** below. Ranking is deterministic (net, then lower acquisition cost, then lower uncertainty, then id). A cheaper near-equivalent can outrank a costlier marginal upgrade on net (tested: a 10.9-pt WR beats an 11.0-pt WR because he is cheaper to acquire, and is surfaced as the alternative).

## 18. Uncertainty
Eight named components with reasons and weights (`role`, `sample`, `projection`, `source_lag`, `pool` certification, `injury_contingency`, `ros_disagreement`, `depth_chart`) → `total ∈ [0,1]` and a value interval (`net_low/net_high`). Not a calibrated interval and labelled as such. An uncertified pool sets `pool = 1`; Role through week 0 vs week 5 raises `source_lag`.

## 19. Deployment strategy
Production `waiver-engine-2026.1` is untouched (six production files pinned by sha256 in a test) and is the only waiver output any production route serves. Waiver 2.0 is reachable **only** through Book-Ready (`waiver2.*`, `SHADOW_ONLY`, `may_influence_production:false`). Shadow runs alongside; nothing is auto-promoted. Activation would need a separate certification with prospective evidence (§21).

## 20. Historical validation
The evaluation suite was **defined before any fitting** (`lib/waiver2/evaluation.ts`: net roster value, starter improvement, replacement gain, breakout identification, false-breakout avoidance, drop regret, FAAB efficiency, opportunity per FAAB, alternative regret) and contains no fitting code. The **evidence audit** classifies what exists: free-agent pool at decision time and candidate ranking = **UNSAFE_FOR_BACKTEST** (never preserved; not materialized); manager roster/FAAB = **RECONSTRUCTABLE_AS_OF** (transaction replay inside the visible window); Role/FI as-of = **RECONSTRUCTABLE_AS_OF** (one preserved week); realized claims and later production = **RETROSPECTIVE_ONLY**; only Waiver 2.0 **LIVE_CAPTURED records with attached outcomes** are **TRUE_AS_OF_HISTORY** (none exist yet). The suite REFUSES anything else. **No backtest was run and no weight is claimed validated** — fabricating waiver pools was rejected.

## 21. Prospective evidence
`capture.ts`: content-addressed `w2cap:` records (pool candidates, roster hash, actions with components' summary and FAAB ranges, evidence identities, scoring fingerprint, model + params hash, evaluation hash). Classes never merge: `LIVE_CAPTURED` (certified pool, pre-lock), `LIVE_POST_LOCK`, `LIVE_UNVERIFIED` (an uncertified pool can never be pristine), `HISTORICALLY_RECONSTRUCTED`. Insert-only, idempotent, tamper-evident store (content-hash check; re-stamped/altered records refused); outcomes attach separately and never mutate a capture; the evidence gate counts only LIVE_CAPTURED + outcome on ≥4 distinct weeks (**NOT_ELIGIBLE today, 0**). A durable-store migration is **prepared, not applied** (`supabase/migrations/20260920190000_waiver2_shadow_captures.sql`), and the capture path is wired into no request path.

## 22. Book-Ready integration
Native, not retrofitted: `waiver2.actions` (net value with decomposition, comparison population `MANAGER_CANDIDATE_ACTIONS`, interval, confidence; drop cost; five horizon blocks; exact lineup delta; uncertainty with 8 reasoned components; role trajectory; conditional OPP as `CONDITIONAL` with `CONDITIONAL_ON`; archetype; realized-vs-opportunity; pass), `waiver2.market` (scarcity, four FAAB rungs as a new `currency` unit kind, priority advice, structural competitor need) and `waiver2.replacement`. Every block validates against the shared contract (real-league run: 79 + 44 + 24 blocks, **0 contract errors**). Registry surface `waiver-intelligence-2` (capability PARTIAL, gated on the pool). With an uncertified pool every topic returns **UNAVAILABLE `FREE_AGENT_POOL_UNAVAILABLE`** unless `illustrative=1`, in which case every block is labelled "ILLUSTRATIVE ONLY … not actionable".

## 23. Analysis Book integration
The three named unsupported capabilities (`replacement_level_evidence`, `drop_cost_evidence`, `manager_competition_evidence`) were **removed from the unsupported table** and the *existing* chapters' needs re-pointed: `decision.replacement_value`, `waiver.drop_cost`, `waiver.manager_competition`, `waiver.availability_faab` (required), `waiver.upside_uncertainty`, `decision.roster_fit` (enrichment). **99 chapters, identical ids, identical Waiver template, no second taxonomy** (tested). Result: those chapters go **UNSUPPORTED → PARTIAL** today (registry-gated on the pool, EXPENSIVE, with the reason stated) and **PARTIAL → READY** the moment the registry declares the surface AVAILABLE — proven by a registry-edit test, with no chapter-identity change. Because chapter needs changed, `TAXONOMY_VERSION` moved to `analysis-taxonomy-2026.2` (contract stays 2026.1); a saved 2026.1 book is reported as drifted, never reinterpreted. The planner warns that waiver2/matchup/start-sit topics each re-run the weekly build. Live in-process: the four waiver chapters block with `FREE_AGENT_POOL_UNAVAILABLE` (honest), not fabricated evidence.

## 24. Performance
Real Bloodline Bowl state (3,120 unrostered candidates → 90 shortlisted, 105 assets): adapter/build **0.7–1.7 s** (canonical state + weekly context + schedule — the existing cost), `evaluateWaiver2` **cold 52 ms / warm 29 ms**, deterministic. Book-Ready in-process: strict (gated) 0.5–1.4 s, illustrative 0.5 s; response sizes **245 KB / 126 KB / 59 KB** (79 / 44 / 24 blocks; lineage repeats per block — the known 3.5C compaction item). Synthetic 120-candidate pool: < 1.5 s ceiling test, ≤ 150 Role lookups. Analysis Book: four waiver chapters plan **3** requests (not 4) and warn about the shared weekly build.

## 25. Tests
Full suite **2321 tests, 2317 pass, 0 fail, 4 skipped**; `tsc --noEmit` clean; `npm run lint` 0 errors (38 pre-existing warnings). New: `waiver2-engine` 23 (determinism/context reuse, roster specificity, transaction completeness, drop cost, scoring, role, fluke, conditional OPP, horizons, optionality, uncertainty, FAAB ×2, alternatives, pass, uncertified pool, adversarial, isolation, performance), `waiver2-bookready` 7, `waiver2-integration` 8 (deployment safety pins, isolation, read-only, FI explanatory, Analysis Book upgrades, taxonomy version), `waiver2-validation` 8, `waiver2-adversarial` 8. No R/model file changed, so no R suite was re-run.

## 26. Live validation (read-only; no claim submitted)
Bloodline Bowl / `supyo29` and Devoted / `darthmarker`, week 2, against real canonical state: pool `UNCERTIFIED_POOL` in both (production reality), Role `roi:2026:w01…`, FI `fi:2026:w02…`, schedule loaded, 12 teams; both evaluations deterministic; **no recommendation** (uncertified pool ⇒ not actionable) with the reason stated; managers rank different candidates and drops (e.g. `supyo29` FAAB `$91` league with visible winning bids → `VISIBLE_WINNING_BIDS`; `darthmarker` is a reverse-standings league → FAAB `NOT_APPLICABLE` and a priority-cost acquisition). The illustrative rankings are **not** actionable and are not presented as such: the real-pool "unrostered" set is every NFL player not on a roster, so candidate quality there is a stress test of the machinery, not advice. Live production `/api/evidence` was not exercised (not deployed).

## 27. Production isolation
Six production files (`waivers.ts`, `decision-score.ts`, `replacement.ts`, `lineup.ts`, `context.ts`, `intelligence.ts`) and the Start/Sit model are byte-identical (sha256-pinned). Only `lib/book-ready/{query.ts,families/waiver2.ts}` import `lib/waiver2`; no weekly/orchestrator/trade/route module names it; Waiver 2.0 has no writes, network, secrets, upward imports or claim-submission path (tests). Existing v1 output is byte-identical before/after Waiver 2.0 runs and inputs are not mutated. **Parity** (interleaved pre-merge `origin/main` vs branch, 3 rounds × 2 managers): lineup, Start/Sit, matchup, leverage and positional needs identical in all 6; waivers identical in 5 of 6 — the one difference (`darthmarker` round 1) is upstream drift: **unmodified `origin/main` disagrees with itself** (`49f66b36…` → `1956952e…` between consecutive runs) and every branch run equals the later one. Production waivers remain `UNAVAILABLE`. Trade math is untouched (`lib/trades/*` unchanged; trade tests in the suite). Files changed outside `lib/waiver2`: registry JSON, Book-Ready (query, schema `currency` kind, units, vocabulary, discovery text), Analysis Book (library, topics, plan warnings, executor reason ordering, taxonomy version).

## 28. Limitations
1. **No weight is validated**: no waiver-pool history exists; all weights/priors are `PRIOR_UNVALIDATED`; the engine stays SHADOW_ONLY.
2. **No live actionable output**: the free-agent pool is not certified in production, so nothing can be recommended; live validation used an uncertified pool.
3. Opportunity volumes and per-target/carry yields are **league-wide priors**; FI has no numeric weight; realized fantasy points are not wired (`recent_points` is optional input — fluke detection is UNAVAILABLE without it).
4. FAAB bids are anchored on an **uncalibrated prior** unless winning bids are visible; other managers' budgets can be missing (labelled).
5. Second-order roster effects (a displaced starter becoming cover) are approximated by the exact next-week lineup delta only for week 1.
6. Threshold scoring bonuses and return scoring are not converted (Phase 6).
7. Analysis Book waiver chapters are PARTIAL (pool-gated); request-scoped topics each rebuild the weekly context (~0.5–1.4 s each).
8. Capture persistence is prepared but not applied or wired; no Book-Ready retrieval of captures.
9. Book-Ready responses repeat lineage per block (245 KB for the actions topic).

## 29. Phase 5 dependencies
Player-specific matchup value (CB/coverage/pressure) to give FI/Player-Scheme numeric weight; validated opportunity volumes and yields per team (pace/PROE); a certified free-agent pool (provider materialization) and, with it, prospective capture in production; expected-points / efficiency evidence for expected-vs-realized; multi-week schedule strength; the Matchup 2.0 chapters that will replace `matchup.*` unsupported capabilities. None was started.

## 30. Certification verdict
**CERTIFIED WITH DOCUMENTED LIMITATIONS.** Gates: manager specificity ✔ (same FA: net 12.33 for a WR-poor roster vs −0.14 for a WR-rich one; reversed for RB); transaction completeness ✔ (add + drop + acquisition + risk, exact lineup); role independent of points ✔ (rising role with no fantasy points raises value; fluke flagged and haircut); replacement above obtainable alternatives ✔; economics ✔ (range from value, alternatives, competition, visible bids — prior labelled); uncertainty explicit ✔; OPP conditional ✔; canonical scoring ✔; Book-Ready native ✔; Analysis Book upgrades without taxonomy duplication ✔; production unchanged and shadow-only ✔; no reconstructed evidence presented as pristine ✔. Limitations §28 are why the verdict is not unconditional. **Not merged, deployed or activated. Phase 5 not started.**
